const BASE_URL = "https://practical-test.test/api/";
const ENDPOINTS = {
  LOGIN: `${BASE_URL}login`,
  LOGOUT: `${BASE_URL}logout`,
  REGISTER: `${BASE_URL}register`,
  EMPLOYEE: `${BASE_URL}employee`,
};

export default ENDPOINTS;
